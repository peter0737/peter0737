- name: enable TEST
  fortiosconfig:
   config: "firewall policy"
   action: "enable"
   host:  "{{ 10.54.5.1 }}"
   username: "{{ noc1 }}"
   password: "{{ Hq-12345 }}"
   config_parameters:
     key: "{{ 86 }}"
